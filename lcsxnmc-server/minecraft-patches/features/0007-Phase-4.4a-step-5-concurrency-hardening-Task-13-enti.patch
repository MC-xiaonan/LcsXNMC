From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: MC-xiaonan <2106104879@qq.com>
Date: Sat, 29 Aug 2026 19:24:58 +0800
Subject: [PATCH] Phase 4.4a step 5: concurrency hardening (Task #13) -
 entityMap/scoreboard sync, ServerWaypointManager mutual exclusion


diff --git a/net/minecraft/server/level/ChunkMap.java b/net/minecraft/server/level/ChunkMap.java
index 7e9361407042ddaabe3f66ee8957ae84b6ef2317..c6642539a5186da7e477041eb8f6251324257b4b 100644
--- a/net/minecraft/server/level/ChunkMap.java
+++ b/net/minecraft/server/level/ChunkMap.java
@@ -139,7 +139,9 @@ public class ChunkMap extends SimpleRegionStorage implements ChunkHolder.PlayerP
     private final ChunkMap.DistanceManager distanceManager;
     private final String storageName;
     private final PlayerMap playerMap = new PlayerMap();
-    public final Int2ObjectMap<ChunkMap.TrackedEntity> entityMap = new Int2ObjectOpenHashMap<>();
+    // LcsXNMC start - region threading: cross-region reads of this map are the norm (Phase 4.4a Task #13), synchronize it
+    public final Int2ObjectMap<ChunkMap.TrackedEntity> entityMap = it.unimi.dsi.fastutil.ints.Int2ObjectMaps.synchronize(new Int2ObjectOpenHashMap<>());
+    // LcsXNMC end - region threading
     private final Long2ByteMap chunkTypeCache = new Long2ByteOpenHashMap();
     // Paper - rewrite chunk system
     public int serverViewDistance;
diff --git a/net/minecraft/server/waypoints/ServerWaypointManager.java b/net/minecraft/server/waypoints/ServerWaypointManager.java
index b9748a392c9f76e61a1b673085b4dd74c007c70d..948a2b92d859b4e855932131763039edc746cffb 100644
--- a/net/minecraft/server/waypoints/ServerWaypointManager.java
+++ b/net/minecraft/server/waypoints/ServerWaypointManager.java
@@ -27,8 +27,10 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
     }
     // Paper end - optimize ServerWaypointManager with locator bar disabled
 
+    // LcsXNMC start - region threading: waypoints/players/connections need cross-collection atomicity
+    // (Phase 4.4a Task #13, per "Task #11 核实结论") - whole-instance mutual exclusion, not per-field
     @Override
-    public void trackWaypoint(final WaypointTransmitter waypoint) {
+    public synchronized void trackWaypoint(final WaypointTransmitter waypoint) {
         this.waypoints.add(waypoint);
 
         if (!this.locatorBarEnabled) return; // Paper - optimize ServerWaypointManager with locator bar disabled
@@ -38,7 +40,7 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
     }
 
     @Override
-    public void updateWaypoint(final WaypointTransmitter waypoint) {
+    public synchronized void updateWaypoint(final WaypointTransmitter waypoint) {
         if (!this.locatorBarEnabled) return; // Paper - optimize ServerWaypointManager with locator bar disabled
         if (this.waypoints.contains(waypoint)) {
             Map<ServerPlayer, WaypointTransmitter.Connection> playerConnection = Tables.transpose(this.connections).row(waypoint);
@@ -55,13 +57,13 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
     }
 
     @Override
-    public void untrackWaypoint(final WaypointTransmitter waypoint) {
+    public synchronized void untrackWaypoint(final WaypointTransmitter waypoint) {
         this.connections.column(waypoint).forEach((player, connection) -> connection.disconnect());
         Tables.transpose(this.connections).row(waypoint).clear();
         this.waypoints.remove(waypoint);
     }
 
-    public void addPlayer(final ServerPlayer player) {
+    public synchronized void addPlayer(final ServerPlayer player) {
         this.players.add(player);
 
         if (this.locatorBarEnabled) { // Paper - optimize ServerWaypointManager with locator bar disabled
@@ -75,7 +77,7 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
         }
     }
 
-    public void updatePlayer(final ServerPlayer player) {
+    public synchronized void updatePlayer(final ServerPlayer player) {
         if (!this.locatorBarEnabled) return; // Paper - optimize ServerWaypointManager with locator bar disabled
         Map<WaypointTransmitter, WaypointTransmitter.Connection> waypointConnections = this.connections.row(player);
         SetView<WaypointTransmitter> potentialWaypoints = Sets.difference(this.waypoints, waypointConnections.keySet());
@@ -89,7 +91,7 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
         }
     }
 
-    public void removePlayer(final ServerPlayer player) {
+    public synchronized void removePlayer(final ServerPlayer player) {
         this.connections.row(player).values().removeIf(connection -> {
             connection.disconnect();
             return true;
@@ -98,21 +100,22 @@ public class ServerWaypointManager implements WaypointManager<WaypointTransmitte
         this.players.remove(player);
     }
 
-    public void breakAllConnections() {
+    public synchronized void breakAllConnections() {
         this.connections.values().forEach(WaypointTransmitter.Connection::disconnect);
         this.connections.clear();
     }
 
-    public void remakeConnections(final WaypointTransmitter waypoint) {
+    public synchronized void remakeConnections(final WaypointTransmitter waypoint) {
         if (!this.locatorBarEnabled) return; // Paper - optimize ServerWaypointManager with locator bar disabled
         for (ServerPlayer player : this.players) {
             this.createConnection(player, waypoint);
         }
     }
 
-    public Set<WaypointTransmitter> transmitters() {
-        return this.waypoints;
+    public synchronized Set<WaypointTransmitter> transmitters() {
+        return java.util.Set.copyOf(this.waypoints);
     }
+    // LcsXNMC end - region threading
 
     // Paper start - optimize ServerWaypointManager with locator bar disabled
     // Avoid a gamerule lookup for each player
diff --git a/net/minecraft/world/scores/Scoreboard.java b/net/minecraft/world/scores/Scoreboard.java
index 4a15851bda30c918c58c96d2cb98b74f30db396c..e3ed4d8c9c2e8be8b9fdb24e95f07a83d1a32356 100644
--- a/net/minecraft/world/scores/Scoreboard.java
+++ b/net/minecraft/world/scores/Scoreboard.java
@@ -30,12 +30,15 @@ import org.slf4j.Logger;
 public class Scoreboard {
     public static final String HIDDEN_SCORE_PREFIX = "#";
     private static final Logger LOGGER = LogUtils.getLogger();
-    private final Object2ObjectMap<String, Objective> objectivesByName = new Object2ObjectOpenHashMap<>(16, 0.5F);
-    private final Reference2ObjectMap<ObjectiveCriteria, List<Objective>> objectivesByCriteria = new Reference2ObjectOpenHashMap<>();
-    private final Map<String, PlayerScores> playerScores = new Object2ObjectOpenHashMap<>(16, 0.5F);
-    private final Map<DisplaySlot, Objective> displayObjectives = new EnumMap<>(DisplaySlot.class);
-    private final Object2ObjectMap<String, PlayerTeam> teamsByName = new Object2ObjectOpenHashMap<>();
-    private final Object2ObjectMap<String, PlayerTeam> teamsByPlayer = new Object2ObjectOpenHashMap<>();
+    // LcsXNMC start - region threading: MinecraftServer.scoreboard is a global singleton touched from per-tick
+    // entity/AI code (Phase 4.4a Task #13); synchronize its maps rather than shard them
+    private final Object2ObjectMap<String, Objective> objectivesByName = it.unimi.dsi.fastutil.objects.Object2ObjectMaps.synchronize(new Object2ObjectOpenHashMap<>(16, 0.5F));
+    private final Reference2ObjectMap<ObjectiveCriteria, List<Objective>> objectivesByCriteria = it.unimi.dsi.fastutil.objects.Reference2ObjectMaps.synchronize(new Reference2ObjectOpenHashMap<>());
+    private final Map<String, PlayerScores> playerScores = java.util.Collections.synchronizedMap(new Object2ObjectOpenHashMap<>(16, 0.5F));
+    private final Map<DisplaySlot, Objective> displayObjectives = java.util.Collections.synchronizedMap(new EnumMap<>(DisplaySlot.class));
+    private final Object2ObjectMap<String, PlayerTeam> teamsByName = it.unimi.dsi.fastutil.objects.Object2ObjectMaps.synchronize(new Object2ObjectOpenHashMap<>());
+    private final Object2ObjectMap<String, PlayerTeam> teamsByPlayer = it.unimi.dsi.fastutil.objects.Object2ObjectMaps.synchronize(new Object2ObjectOpenHashMap<>());
+    // LcsXNMC end - region threading
 
     public @Nullable Objective getObjective(final @Nullable String name) {
         return this.objectivesByName.get(name);
